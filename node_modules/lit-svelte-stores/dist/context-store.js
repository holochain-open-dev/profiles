import { decorateProperty } from "@lit/reactive-element/decorators/base.js";
import { ContextStoreController } from "./context-store-controller";
export function contextStore({ context, selectStore, }) {
    return decorateProperty({
        finisher: (ctor, name) => {
            ctor.addInitializer((element) => {
                element.addController(new ContextStoreController(element, context, (value) => {
                    // eslint-disable-next-line @typescript-eslint/no-explicit-any -- have to force the property on the type
                    element[name] = value;
                }, selectStore));
            });
        },
    });
}
//# sourceMappingURL=context-store.js.map