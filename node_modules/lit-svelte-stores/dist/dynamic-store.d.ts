import { Readable, Unsubscriber } from "svelte/store";
import { ReactiveController, ReactiveElement } from "lit";
/**
 * Tracks a changing store, derived at each update
 */
export declare class DynamicStore<V, El extends ReactiveElement> implements ReactiveController {
    protected host: El;
    protected getStore: () => Readable<V>;
    value: V;
    _unsubscribe: Unsubscriber;
    private _previousStore;
    constructor(host: El, getStore: () => Readable<V>);
    hostUpdate(): void;
    hostDisconnected(): void;
    resubscribe(): void;
}
