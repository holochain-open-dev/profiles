import { Context, ContextConsumer } from "@lit-labs/context";
import { ReactiveElement } from "@lit/reactive-element";
import { StoreController } from "./store-controller";
import { Readable } from "svelte/store";
export declare class ContextStoreController<V, HostElement extends ReactiveElement, Ctx = Readable<V>> extends ContextConsumer<Ctx, HostElement> {
    storeController: StoreController<V>;
    get value(): V;
    constructor(host: HostElement, context: Context<Ctx>, callback: (value: V) => any, selectStore?: (context: Ctx) => Readable<V>);
}
