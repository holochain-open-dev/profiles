import { ReactiveElement } from "@lit/reactive-element";
import { Readable } from "svelte/store";
import { Context } from "@lit-labs/context";
export declare function contextStore<V, Ctx>({ context, selectStore, }: {
    context: Context<Ctx>;
    selectStore?: (context: Ctx) => Readable<V>;
}): <K extends PropertyKey>(protoOrDescriptor: ReactiveElement & Record<K, V>, name?: K) => void | any;
